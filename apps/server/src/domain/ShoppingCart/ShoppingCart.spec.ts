describe('ShoppingCart', () => {
  describe('addCheckout', () => {
    xit('when empty, adds and sets the loadedAt time', () => {});
    xit('when already loaded, adds', () => {});
  });

  describe('isLoaded', () => {
    xit('when it has any item, returns true', () => {});
    xit('when it is empty, returns false', () => {});
  });

  describe('empty', () => {
    xit('empties correctly', () => {});
  });
});
